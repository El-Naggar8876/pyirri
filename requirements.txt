# OpenIrri runtime dependencies — pinned for reproducibility (Python >= 3.11).
# These exact versions correspond to the v1.0.2 release archived on Zenodo.
streamlit==1.40.2
pandas==2.2.3
numpy==1.26.4
scipy==1.13.1
plotly==5.24.1
openpyxl==3.1.5                 # Excel (.xlsx) report export via pandas
folium==0.17.0
streamlit-folium==0.23.1
streamlit-plotly-events==0.0.6
geopy==2.4.1
shapely==2.0.6                  # field-polygon geometry
ezdxf==1.3.5                    # DXF layout export
pyyaml==6.0.2
earthengine-api==1.4.3          # optional: Google Earth Engine cloud storage / climate retrieval
